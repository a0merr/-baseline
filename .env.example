# Copy this file to `.env` and adjust. Never commit the real `.env`.

APP_NAME=baseline
ENVIRONMENT=development
DEBUG=false

# Example service config — delete if your project has no database.
DATABASE_URL=postgresql://baseline:baseline@localhost:5432/baseline
